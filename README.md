# Containers collection

### Requirements

- [Docker](https://docker.com)

### Usage

1. Run all containers with `docker compose up`
2. Run a single container with `docker compose up [container_name`
3. Run in the background `docker compose up -d`